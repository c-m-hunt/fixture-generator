[project]
name = "fix-gen"
version = "0.1.0"
description = "Cricket league fixture generator with constraint satisfaction"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ortools>=9.8",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
]
